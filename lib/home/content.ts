/** Static copy and assets for the home page. */
export const HOME_HERO_IMAGE = "/home/hero.jpg";

export const HOME_HERO_COPY = "White, only.";

export const HOME_CONCEPT_LINES = [
  "White is not empty.",
  "It is what remains when everything else is removed.",
] as const;

export const FEATURED_PRODUCT_COUNT = 3;

/** Fabric cards shown on the home page before linking to the full list. */
export const HOME_FABRIC_PREVIEW_COUNT = 3;
